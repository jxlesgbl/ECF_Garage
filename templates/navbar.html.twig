{% block body %}

<nav class="navbar navbar-expand-lg navbar-light bg-light" data-controller="navbar">
    <div class="container">
        <a class="navbar-brand" href="/"><img width="100px" src="/images/logo.png"></img></a>
        <button data-action="click->navbar#toggleMenu" class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div data-target="navbar.menu" class="collapse navbar-collapse menu" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_car_list') }}">Voitures</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_service') }}">Prestations</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('contact') }}">Contact</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('testimonial_list') }}">Avis clients</a>
                </li>
                <!-- Add more menu items as needed -->
                <span class="login">
                {% if is_granted('ROLE_ADMIN') %}
                    <a class="btn btn-success" href="{{ path('app_register') }}">Nouveau compte</a>
                {% endif %}
                {% if is_granted('ROLE_USER') %}
                    <a class="btn btn-light" href="{{ path('app_logout') }}">Déconnexion</a>
                {% else %}
                    <a class="btn btn-success" href="{{ path('app_login') }}">Espace Pros</a>
                {% endif %}
                </span>
            </ul>

        </div>
    </div>
</nav>


{% endblock %}
